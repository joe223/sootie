# Verification Matrix

This matrix tracks the current implementation against the active Sootie
completion criteria. It is intentionally evidence-based: a green test is useful
only when it covers the requirement it is cited for.

## Success Criteria

1. Sootie is implemented as an independent Rust repository.
2. The public MCP surface uses Sootie tool names while keeping compatible data
   and argument shapes.
3. The feature surface includes desktop perception, actions, waits, recipes,
   visual grounding, annotation, learning, and browser automation.
4. The implementation supports macOS, Linux, and Windows backends.
5. Browser targets support CDP when remote debugging is enabled; CDP is the
   only intentional capability beyond the selected public tool contract.
6. macOS runtime behavior is verified through the real MCP stdio path.
7. Linux and Windows code paths compile and lint cleanly; real desktop runtime
   tests remain external to this machine.

## Current Audit Result

As of 2026-05-17, the active acceptance scope has current evidence. The Rust
implementation, public tool contract, local macOS runtime path, release MCP
stdio path, workspace tests, package contents, documentation placement, and
client configuration gates have current evidence. A production panic audit also
found no Rust `unwrap`, `expect`, `panic`, `todo`, or `unimplemented` use outside
test modules; the only remaining text hit is `ObjC.unwrap` inside a macOS JXA
script string. The current acceptance scope requires full macOS runtime
evidence, while Linux and Windows require implementation plus build/link
evidence. The final aggregate verifier passes with macOS runtime evidence at
`/private/tmp/sootie-macos-runtime-evidence2/macos-evidence.json`, Linux
build-only evidence at `/private/tmp/sootie-linux-build-evidence5/linux-evidence.json`,
and Windows build-only evidence at
`/private/tmp/sootie-windows-build-evidence2/windows-evidence.json`.

The third-party client matrix still keeps Claude Code fresh-session tool-call
evidence as a separate external-client follow-up because local Claude CLI
session/auth/network startup blocked before a Sootie tool invocation. That gap
does not change the core Sootie runtime/build acceptance result.

`node docs/development/verify-runtime-evidence.mjs` is the final evidence
aggregator. For the current acceptance scope it must pass with macOS in runtime
mode and Linux/Windows in build-only mode:

```bash
node docs/development/verify-runtime-evidence.mjs \
  --evidence path/to/macos-evidence.json \
  --evidence path/to/linux-evidence.json \
  --evidence path/to/windows-evidence.json \
  --build-only linux \
  --build-only windows
```

A one-platform `--platform` run is only a partial audit.

## Prompt-to-Artifact Checklist

| Requirement | Artifact or command | Coverage status |
| --- | --- | --- |
| Independent Sootie Rust implementation, not changes to any external codebase | Workspace manifests, `crates/sootie-core`, `crates/sootie-cli`, and deleted legacy sidecar/setup surfaces in the current diff. | Implemented in this repo |
| Public APIs use Sootie names, not compatibility-target names | `target/release/sootie tools --raw` reports 58 tools; `tools::tests::tool_contract_is_exact` fixes the tool list; [MCP Tools Reference](../api/mcp-tools-reference.md) documents the public surface. | Verified |
| Keep compatible parameter and data shapes while changing API names | `tools::tests::tool_contract_is_exact`, `tools::tests::property_types_match_public_compatibility_contract`, `tools::tests::advertised_shapes_do_not_expand_public_compatibility_parameters`, `tools::tests::required_fields_match_public_compatibility_contract`, `node docs/development/verify-public-tool-contract.mjs`, MCP envelope tests, direct public argument-name, required-field, and value-type rejection tests, finite coordinate tests, pointer button, scroll direction, count, amount, duration, crop-box, annotation-label, recipe JSON-string, and [MCP Tools Reference](../api/mcp-tools-reference.md). The portable `*` surface stays compatibility-shaped; browser-native `browser_*` and raw `cdp_*` tools intentionally advertise browser-specific fields such as `page_id`, `timeout_ms`, `include`, and guarded `unsafe` flags. Missing advertised required fields are rejected before dispatch. | Verified |
| Functional surface matches the selected compatibility target except for the intentional browser-native CDP path | `tools::tests::tool_contract_is_exact` plus the 58-tool `tools` output define the public tool contract. Portable `*` calls keep the existing fallback model; browser-native `browser_*` calls are CDP-only and raw `cdp_*` calls are guarded escape hatches. | Verified for the selected tool contract plus CDP |
| macOS support | macOS backend implementation and unit coverage exist. 2026-05-17 release `target/release/sootie doctor --check --raw` through the real desktop launch path reports `runtime_ready: true`, `runtime_blockers: []`, `context_app: "TextEdit"`, `context_window: "sootie-smoke-target.txt"`, screenshot size `3456x2234`, and successful macOS Accessibility plus WindowServer diagnostics. The current browser-native expansion requires a 58-tool smoke with a CDP-enabled browser for complete runtime evidence. | Verified |
| Linux support | Linux backend implementation exists, passes target clippy, and exposes `doctor` diagnostics for X11, automation, accessibility, and screenshot prerequisites. GNU target builds on this macOS host still need a Linux sysroot, but the official `x86_64-unknown-linux-musl` target builds with Rust's bundled `rust-lld`. The refreshed build-only bundle at `/private/tmp/sootie-linux-build-evidence5/linux-evidence.json` passes the runtime evidence verifier with native build/test/clippy gates and target build/test-no-run/clippy gates. | Build-only acceptance verified |
| Windows support | Windows backend implementation exists, passes target clippy, and exposes `doctor` diagnostics for PowerShell, UI Automation, Forms/Drawing, and visible-window prerequisites. MSVC builds on this macOS host still require `link.exe` and Windows SDK import libraries, and GNU builds require a MinGW runtime. After provisioning a temporary llvm-mingw toolchain under `/private/tmp`, the official `x86_64-pc-windows-gnullvm` target builds and test-harness links successfully. The refreshed build-only bundle at `/private/tmp/sootie-windows-build-evidence2/windows-evidence.json` passes the runtime evidence verifier with native build/test/clippy gates and target build/test-no-run/clippy gates. | Build-only acceptance verified |
| Browser/CDP answer and implementation | `docs/guides/browser-cdp.md`, `backend::cdp` tests, serialized CDP TCP mock coverage for Runtime.evaluate hover/long-press/drag, and the 2026-05-16 release-binary CDP smoke against headless Chrome. | Verified |
| Tests prove functionality | `cargo test --workspace`, native target clippy, cross-target clippy, package, release CLI checks, real MCP CDP smoke, macOS full-runtime evidence, and Linux/Windows build-only evidence. | Verified for current acceptance scope |
| Repository contains no forbidden external keyword | `node docs/development/verify-repository-independence.mjs` scans product paths and file contents, while a hidden/no-ignore keyword scan excluding generated state, build output, VCS metadata, and lockfile output returns no matches. A temporary negative fixture verified that both path-name matches and content matches fail the script. | Verified |

## Evidence

| Area | Evidence | Status |
| --- | --- | --- |
| Rust workspace | `Cargo.toml` contains `sootie-core` and `sootie-cli`; `cargo metadata --no-deps --format-version 1` reports exactly those two workspace members. 2026-05-17 post-macOS-AX refresh: `cargo build --release`, `cargo test --workspace` with 12 CLI tests and 165 core tests, native `cargo clippy --workspace --all-targets -- -D warnings`, Linux target clippy, Windows target clippy, and `git diff --check` pass. Earlier verification also covered `cargo doc --workspace --no-deps` and packaging. | Verified |
| MCP transport | `mcp::tests::stdio_accepts_content_length_framed_requests`, `mcp::tests::stdio_keeps_line_json_compatibility`, `mcp::tests::tools_list_serializes_mcp_annotations`, and `mcp::tests::context_error_can_include_runtime_diagnostic_recovery`. Current `tools/list` exposes 58 tools, including browser-native CDP tools and guarded raw CDP escape hatches. The latest line-json smoke also verifies public value-type rejection for advertised schemas, including `anyOf` browser scroll amount validation. | Verified |
| Tool surface | `tools::tests::tool_contract_is_exact`, `tools::tests::property_types_match_public_compatibility_contract`, `tools::tests::advertised_shapes_do_not_expand_public_compatibility_parameters`, `mcp::tests::advertised_tools_all_dispatch_to_tool_reports`, string-array/schema tests, `node docs/development/verify-public-tool-contract.mjs`, and `target/release/sootie tools --raw` expose 58 tools; the dispatch test confirms every advertised tool can enter the standard `tools/call` report path. [full-tool-smoke-requests.jsonl](full-tool-smoke-requests.jsonl) now includes portable, browser-native, and guarded raw CDP tools. `node docs/development/verify-full-tool-smoke.mjs` remains the reusable verifier for that contract. | Verified |
| Runtime evidence gate | `node docs/development/verify-runtime-evidence.mjs` reads the expected public tool count from `TOOL_NAMES` and validates per-platform evidence JSON for macOS, Linux, and Windows. Runtime-mode platforms fail on missing or duplicate required platforms, failed build/client/smoke markers, missing build log artifacts for passing build gates, build logs without `exit_status=0`, non-empty runtime blockers, `unknown` or `loginwindow` context apps, missing screenshot dimensions, wrong tool count, missing artifact files, empty runtime logs, missing or invalid framed JSON-RPC logs, framed logs without initialize, framed `tools/list` with all tools and `learn_status.readOnlyHint: true`, plus successful tool reports. Browser-native evidence requires a CDP-enabled browser for the `browser_*` and `cdp_*` smoke rows. | Verified with final target artifacts |
| Argument compatibility | `tools::tests::required_fields_match_public_compatibility_contract`, `tools::tests::advertised_shapes_do_not_expand_public_compatibility_parameters`, `mcp::tests::tool_call_accepts_compatible_argument_envelopes`, `mcp::tests::tool_call_accepts_top_level_data_and_input_envelopes`, `mcp::tests::public_tool_calls_reject_values_outside_advertised_schema_types`, `mcp::tests::public_tool_calls_reject_missing_advertised_required_args_before_dispatch`, and direct rejection coverage for unadvertised `target`, `to_app`, `bounds`, `fullResolution`, `clear_first`, `timeout_ms`, `interval_ms`, `duration_ms`, `hold_duration_ms`, `title`, `url`, `roles`, `max_candidates`, and `recipe` aliases. A release-binary MCP smoke also verifies value-type rejection over stdio. The dispatcher remains tolerant of older aliases internally for recipe migration helpers, but direct public MCP `tools/call` now rejects fields, missing advertised required fields, and JSON value types outside the public compatibility Sootie tool schema before dispatch. | Verified |
| App identity fields | `mcp::tests::state_tool_returns_app_identity_fields` covers `app`, `app_id`, `platform_app_id`, `bundle_id`, and filtered-state `app_count`. 2026-05-16 release-binary MCP smoke verified `state(app=Calculator)` returns stable `app_count` plus `apps` even when the filtered app is not running. | Verified |
| macOS runtime perception | Earlier real MCP smokes verified `context`, `find`, `read`, `inspect`, `parse_screen`, `annotate`, `ground`, `element_at`, and `screenshot`. 2026-05-17 refresh moved the runtime readiness check away from the older System Events Automation probe and onto current macOS boundaries: Swift Accessibility and WindowServer. The final macOS evidence bundle uses a disposable TextEdit document and verifies `doctor --check --raw` with `runtime_ready: true`, no blockers, `context_app: "TextEdit"`, `context_window: "sootie-smoke-target.txt"`, screenshot size `3456x2234`, all perception tools in the raw MCP log, and screenshot PNG dimensions matching the doctor summary. | Verified |
| macOS runtime action | 2026-05-16 release-binary MCP smokes verified `hover` and coordinate `click` through the real desktop permission path; both returned `success: true` and `method: "coregraphics"`. Later permissioned release MCP smokes verified `focus`, `type`, `press`, `hotkey`, `scroll`, `long_press`, `drag`, `window`, `wait`, and follow-up `context`; all returned `success: true`. 2026-05-17 implementation reduced System Events dependency for common action paths: `type` now sets text with `pbcopy` and posts CoreGraphics `Command+V`; `press` and `hotkey` now post CoreGraphics keyboard events; `focus(app=...)` first attempts a Swift/AppKit activation path; and window-specific focus/restore/minimize/maximize/close/move/resize now use Swift Accessibility window actions. The final macOS evidence raw MCP log verifies the action tools against a disposable TextEdit window with no failed reports. Unit tests cover the CoreGraphics keyboard scripts, Swift/AppKit activation script shape, Swift AX window action script shape, Swift AX element script shape, and unknown-key/modifier rejection. | Verified |
| macOS window fallback | Real MCP smoke verified WindowServer fallback for an app with no AX window tree; unit tests cover fallback parsing and merge behavior. | Verified |
| macOS runtime CDP | 2026-05-16 release-binary MCP smoke verified browser DOM `find`, `type`, `click`, `wait`, and post-click `read` against a temporary headless Chrome remote-debugging profile on port 9336. `type` and `click` returned `cdp-type` and `cdp-click`; the final read observed the click-generated DOM text. Earlier smoke also verified CDP screenshot capture. | Verified |
| Recipes | 2026-05-16 release-binary MCP smoke with a temporary home directory verified `recipe_save`, `recipe_show`, `recipes`, `run`, and `recipe_delete`; the saved `smoke-delay` recipe ran one wait step successfully. [Recipe Schema](../api/recipe-schema.md) documents the saved JSON format, and a recipe action map audit confirmed the documented table exactly matches the supported legacy action set: 13 actions, no missing, extra, or duplicate entries. | Verified |
| Learning | 2026-05-16 release-binary MCP smoke verified `learn_start`, a successful `window` list action, `learn_status`, and `learn_stop`; the stopped recording returned one `window` action. | Verified |
| CLI | `cli_accepts_documented_commands`, `tools_json_preserves_mcp_annotations`, and platform-note tests for Linux/Windows runtime prerequisites; `cargo run -p sootie-cli -- --help` verifies the source entrypoint. Current CLI `tools --raw` returns 58 tools with annotations, including browser-native CDP and raw CDP escape-hatch tools. | Verified |
| CLI diagnostics | `target/release/sootie doctor` now prints a readable readiness summary, while `target/release/sootie doctor --raw` reports `launch_context`, `context_app`, `context_window`, `context_element_count`, `screenshot_available`, `screenshot_size`, `screenshot_error`, `runtime_ready`, `runtime_blockers`, and `runtime_diagnostics`; `target/release/sootie doctor --check` exits non-zero when the runtime is not ready. 2026-05-17 refresh updates macOS diagnostics to match the current implementation boundaries: `macos_accessibility` probes AX window access with Swift/ApplicationServices, and `macos_window_server` probes visible windows. A release `doctor --check --raw` through the real desktop path returned `runtime_ready: true`, `runtime_blockers: []`, `context_element_count: 80`, screenshot size `3456x2234`, `macos_accessibility.success: true`, and `macos_window_server.success: true`. Native, Linux-target, and Windows-target clippy pass after the diagnostic update. | Verified |
| MCP client attachment diagnostics | 2026-05-16 direct line-json and `Content-Length` smokes passed, `codex mcp get sootie` reported the stdio server enabled with `target/release/sootie serve`, and fresh Codex CLI sessions successfully called `learn_status` and `state`. Project `.mcp.json` uses the Claude Code `mcpServers` schema with `target/release/sootie serve`; a post-CDP-test-hardening config check verified `type: "stdio"`, command `target/release/sootie`, args `["serve"]`, and an existing command path; post-refresh `codex mcp get sootie` reports `enabled: true`, `transport: stdio`, command `/Users/bytedance/git/sootie/target/release/sootie`, and args `serve`; post-refresh `claude mcp get sootie` reports project config `Status: Connected`. The latest Sootie log for Claude Code health checks still shows `initialize`, `notifications/initialized`, `tools/list`, `resources/list`, and `resources/templates/list`, but no `tools/call`, so the failed fresh-session attempt did not reach Sootie as a tool invocation. Earlier no-MCP/no-tool Claude controls failed with `FailedToOpenSocket`; a post-CDP-test-hardening no-MCP control hung without output in both normal and escalated launch paths for more than 20 seconds and was stopped, so the current blocker is still Claude CLI session startup/auth/network rather than Sootie dispatch. Release `tools/list` output now includes MCP annotations, including `readOnlyHint: true` for read-only tools, so the previous missing-annotation approval path is not the current blocker. The `state` smoke returned `success: true`, `app_count: 19`, and active app `ariatype`. The Sootie log for verified Codex calls recorded `initialize`, `notifications/initialized`, `tools/list`, `tools/call`, and successful tool completion. | Verified for Codex and Claude project health check |
| Documentation links | 2026-05-16 post-runbook-refresh README/docs link check verified all relative Markdown link targets across 8 Markdown files exist, with `missing: []`. | Verified |
| Documentation placement | 2026-05-16 Markdown placement audit verified the only root Markdown files are `AGENTS.md` and `README.md`; all other Markdown files live under `docs/`, with no misplaced root or nested non-docs Markdown. | Verified |
| License metadata | 2026-05-16 post-CDP-test-hardening metadata and license check verified workspace members are exactly `sootie-cli` and `sootie-core`, both crates report `license = "MIT OR Apache-2.0"`, and `LICENSE` includes both MIT and Apache-2.0 texts. | Verified |
| Linux backend | 2026-05-17 `cargo clippy --workspace --all-targets --target x86_64-unknown-linux-gnu -- -D warnings` passed after the macOS AX update. Native GNU target builds reached link but failed on missing Linux sysroot/linker support. After installing the official `x86_64-unknown-linux-musl` target, `RUSTFLAGS='-C linker=rust-lld' cargo build --workspace --target x86_64-unknown-linux-musl` completed successfully. The refreshed Linux build-only bundle at `/private/tmp/sootie-linux-build-evidence5/linux-evidence.json` passes `node docs/development/verify-runtime-evidence.mjs --platform linux --build-only linux`, including `cargo_build_workspace`, `cargo_test_workspace`, `cargo_clippy_workspace`, `target_build`, `cargo_test_no_run`, `target_clippy`, and `target_test_no_run` artifacts with `exit_status=0`. | Build/lint verified |
| Windows backend | 2026-05-17 `cargo clippy --workspace --all-targets --target x86_64-pc-windows-msvc -- -D warnings` passed after the macOS AX update, and `cargo clippy --workspace --all-targets --target x86_64-pc-windows-gnu -- -D warnings` also passed after reducing CLI color/ANSI dependency features. `cargo build --workspace --target x86_64-pc-windows-msvc` still reaches link but fails because `link.exe` and Windows SDK import libraries are unavailable on this macOS host. `x86_64-pc-windows-gnu` gets past the previous `windows-sys`/`dlltool` dependency edge after disabling color/ANSI features, but Rust's GNU target expects `libgcc` inputs that the temporary llvm-mingw UCRT toolchain does not provide. The `x86_64-pc-windows-gnullvm` target matches the clang/lld toolchain and completes `cargo build --workspace --target x86_64-pc-windows-gnullvm`. The refreshed Windows build-only bundle at `/private/tmp/sootie-windows-build-evidence2/windows-evidence.json` passes `node docs/development/verify-runtime-evidence.mjs --platform windows --build-only windows`, including `cargo_build_workspace`, `cargo_test_workspace`, `cargo_clippy_workspace`, `target_build`, `cargo_test_no_run`, `target_clippy`, and `target_test_no_run` artifacts with `exit_status=0`. | Build/lint verified |
| Packaging | `cargo package --workspace --no-verify --allow-dirty --offline` packages both crates; `sootie-cli` depends on `sootie-core` with both `path` and `version`. 2026-05-16 package refresh produced `sootie-core` with 15 files, 534.3 KiB uncompressed and 97.5 KiB compressed, plus `sootie-cli` with 5 files, 30.1 KiB uncompressed and 9.0 KiB compressed; the generated crate archives contain only the current core/CLI source files plus Cargo metadata, and extracted package contents pass the keyword hygiene scan. | Verified |
| Repository hygiene | Hidden/no-ignore keyword scan excluding `target` and `.git` returns no matches. The production panic audit command excluding `#[cfg(test)]` modules found no Rust `unwrap`, `expect`, `panic`, `todo`, or `unimplemented` use in production source; the only text hit was `ObjC.unwrap` inside a macOS JXA script string. | Verified |

## Remaining Gaps

| Gap | Why it remains open |
| --- | --- |
| macOS restricted-shell caveat | The real desktop launch path is ready and passes `doctor --check --raw`. A sandboxed shell that is not attached to the active Aqua desktop can still report `unknown` app state, no visible WindowServer windows, or screenshot capture failure. Treat that as an execution-environment limitation: rerun through the same launcher that owns Accessibility and Screen Recording permissions before judging macOS runtime readiness. |
| macOS per-target Automation prompts | A later 2026-05-16 permissioned release smoke successfully launched and focused Calculator, then confirmed it via `context`. macOS may still require per-target Automation approval for other apps; when that boundary appears, accept the system prompt, restart the launcher if needed, and retry. |
| Linux runtime verification | This machine cannot run a Linux desktop session for `wmctrl`, `xdotool`, screenshot utilities, and browser process checks, and full Linux runtime is outside the current acceptance scope. Build-only acceptance is covered by the `x86_64-unknown-linux-musl` evidence bundle at `/private/tmp/sootie-linux-build-evidence5/linux-evidence.json`; collect full runtime evidence later on a Linux desktop if that scope expands. |
| Windows runtime verification | This machine cannot run a Windows interactive desktop session for PowerShell, User32, UI Automation, and System.Drawing checks, and full Windows runtime is outside the current acceptance scope. Build-only acceptance is covered by the `x86_64-pc-windows-gnullvm` evidence bundle at `/private/tmp/sootie-windows-build-evidence2/windows-evidence.json`; collect full runtime evidence later on a Windows desktop if that scope expands. |
| Third-party MCP client matrix | Sootie has stdio protocol coverage, direct smoke scripts, MCP tool annotations, verified Codex CLI attachment, a Claude Code project-config health check through `.mcp.json`, and a client evidence matrix in [Runtime Smoke Runbook](runtime-smoke-runbook.md). A bounded Claude Code fresh-session command is documented, but local attempts did not produce a Sootie `tools/call`. Earlier no-MCP Claude Code control prompts failed with `FailedToOpenSocket`; the latest no-MCP controls hung without output in both normal and escalated launch paths and had to be stopped. The local blocker remains Claude CLI auth/network/session startup rather than Sootie dispatch. Broad client-specific fresh-session tool-call testing beyond Codex CLI remains external; do not mark those clients verified until their read-only call evidence is captured. |

The remaining gaps are outside the current acceptance scope or tied to
third-party client startup on this machine; they do not block the core Sootie
runtime/build objective described above.
